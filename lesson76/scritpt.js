const firstName = 'Иван'
    , lastName = 'Манохин'
    , fatherName = 'Павлович'
    , age = 21;

let   message = 'Hello, im using JS!'
    , action = ' (Wawes his hand)';
alert("Переменные let и const отличаются тем, что переменную let можно изменять, а переменную const - нельзя.");

console.log("ФИО: "+lastName+" "+firstName+" "+ fatherName);
console.log("Возраст: "+age+" год");
console.log(message+action);
